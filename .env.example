# PostgreSQL connection string
# Local: postgresql://USER:PASSWORD@localhost:5432/rent_receipts?schema=public
# Vercel Postgres (Neon): use the pooled connection string
# Supabase: use the transaction pooler URL (port 6543)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/rent_receipts?schema=public"